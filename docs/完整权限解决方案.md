# 微信小程序订阅消息权限完整解决方案

## 权限错误 -604101 分析

### 错误含义
`-604101` 表示云函数没有权限调用订阅消息API，这是微信小程序最新的权限控制机制。

## 根本解决方案

### 第一步：小程序配置与认证

#### 1. 确认小程序状态
```bash
# 检查小程序基本状态
- 小程序必须通过微信官方审核
- AppID: wx5783986edcbaba40 需要有效
- 小程序必须开通订阅消息功能
```

#### 2. 开发者身份绑定（关键步骤）
```bash
# 必须完成开发者绑定
1. 访问 https://mp.weixin.qq.com/
2. 使用小程序管理员账号登录
3. 成员管理 → 添加开发者
4. 输入您当前登录开发者工具的微信号
5. 设置为"管理员"权限（非订阅者）
6. 在微信中确认邀请
7. 重新登录开发者工具验证权限
```

#### 3. 订阅消息功能开通
```bash
# 微信公众号后台配置
1. 功能 → 订阅消息 → 开通功能
2. 添加订阅消息模板
3. 模板状态必须为"审核通过"
4. 确认模板ID: tLEKQiiMe8JDjm1GIGq5UDbHrZNZX0bxOhuRM0zho4g
```

### 第二步：云开发环境配置

#### 1. 云开发基础配置
```javascript
// app.js 配置
wx.cloud.init({
  env: 'cloud1-5ga4h58zc0ea35dc', // 确认环境ID正确
  traceUser: true
})
```

#### 2. 云开发权限检查
```bash
# 云开发控制台权限检查
1. 云开发控制台 → 设置 → 权限设置
2. 开启"微信API调用"权限
3. 开启"订阅消息"权限
4. 确认"服务商"权限（如适用）
```

#### 3. 云函数权限配置
```javascript
// cloudfunctions/sendMessage/index.js
// 确保使用正确的SDK版本
"dependencies": {
  "wx-server-sdk": "~2.6.3"
}

// 确保云函数有正确的权限声明
const cloud = require('wx-server-sdk')
cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})
```

### 第三步：订阅消息API正确使用

#### 1. 用户授权检查
```javascript
// utils/authorization.js 确保授权逻辑正确
function requestSubscribeMessage(templateId) {
  return new Promise((resolve, reject) => {
    wx.requestSubscribeMessage({
      tmplIds: [templateId],
      success: (res) => {
        // 检查授权状态
        console.log('订阅消息授权结果:', res)
        if (res[templateId] === 'accept') {
          resolve({ success: true })
        } else {
          reject({ success: false, message: '用户拒绝接收消息' })
        }
      },
      fail: reject
    })
  })
}
```

#### 2. 云函数正确实现
```javascript
// cloudfunctions/sendMessage/index.js
exports.main = async (event, context) =><｜tool▁sep｜>new_string
      // 步骤4：复制消息到剪贴板
      copyMenuToClipboard(messageData)
    })
}
