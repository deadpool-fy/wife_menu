# 深度权限排查指南

## 已确认的配置
- ✅ 小程序管理员身份
- ✅ 令牌权限配置：`/wxa/openapi`

## 进一步排查步骤

### 第一步：检查订阅消息模板状态

1. **微信公众平台检查**：
   - 访问：https://mp.weixin.qq.com/
   - 功能 → 订阅消息
   - 确认模板ID `tLEKQiiMe8JDjm1GIGq5UDbHrZNZX0bxOhuRM0zho4g` 状态为 **"审核通过"**

2. **模板字段验证**：
   - 确认模板包含字段：`thing1`、`time2`、`thing4`
   - 如果字段不匹配，需要修改云函数代码

### 第二步：检查云开发环境配置

1. **环境ID确认**：
   ```javascript
   // app.js 中的环境ID
   env: 'cloud1-5ga4h58zc0ea35dc'
   ```
   - 确认与云开发控制台中的环境ID一致

2. **云函数部署状态**：
   - 确认云函数状态为 **"正常"**
   - 检查是否有部署错误

### 第三步：检查用户授权状态

1. **清除小程序数据**：
   - 微信 → 发现 → 小程序 → 家庭菜单
   - 右上角设置 → 清除数据

2. **重新授权测试**：
   - 进入小程序
   - 选择菜品 → 发送
   - 确认授权弹窗出现

### 第四步：尝试更具体的API权限

如果 `/wxa/openapi` 不行，尝试添加更具体的API：

```
wxa/openapi/subscribeMessage/send
```

或者：

```
subscribeMessage/send
```

### 第五步：检查云函数日志

1. **查看详细日志**：
   - 云开发控制台 → 云函数 → sendMessage → 日志
   - 查看是否有其他错误信息

2. **本地调试测试**：
   - 右键云函数 → 本地调试
   - 输入测试参数验证

### 第六步：尝试简化测试

创建一个最简单的测试云函数：

```javascript
// cloudfunctions/testPermission/index.js
const cloud = require('wx-server-sdk')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

exports.main = async (event, context) => {
  try {
    // 测试最简单的API调用
    const result = await cloud.openapi.subscribeMessage.send({
      touser: 'test_openid',
      template_id: 'your_template_id',
      page: 'pages/index/index',
      data: {
        thing1: { value: '测试' },
        time2: { value: '2025-09-29' },
        thing4: { value: '测试消息' }
      }
    })
    
    return {
      success: true,
      result: result
    }
  } catch (error) {
    return {
      success: false,
      error: error.message,
      errCode: error.errCode
    }
  }
}
```

### 第七步：检查其他可能原因

1. **小程序版本问题**：
   - 确认基础库版本 ≥ 2.2.3
   - 检查是否有版本兼容问题

2. **网络问题**：
   - 尝试不同的网络环境
   - 检查是否有防火墙限制

3. **微信服务器问题**：
   - 查看微信官方公告
   - 确认服务是否正常

## 临时解决方案

如果权限问题持续存在，可以考虑：

### 方案1：使用统一服务消息
```javascript
// 需要申请统一服务消息权限
await cloud.openapi.uniformMessage.send({
  touser: targetUserId,
  weapp_template_msg: {
    template_id: templateId,
    page: 'pages/index/index',
    data: templateData
  }
})
```

### 方案2：使用客服消息
```javascript
// 需要开通客服功能
await cloud.openapi.customerServiceMessage.send({
  touser: targetUserId,
  msgtype: 'text',
  text: {
    content: message
  }
})
```

### 方案3：降级到复制功能
保持当前的复制到剪贴板功能作为备用方案。

## 下一步建议

1. **立即检查**：订阅消息模板状态
2. **尝试添加**：更具体的API权限 `wxa/openapi/subscribeMessage/send`
3. **创建测试**：简化版云函数验证权限
4. **查看日志**：云开发控制台的详细错误信息

请告诉我您检查的结果，我们可以进一步定位问题。
